{% extends 'base.html.twig' %}

{% block main %}
    <h1>User_comment creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('user_show', {'id' : user_Comment.user.id}) }}">Back to the user</a>
        </li>
    </ul>
{% endblock %}
