#include <unistd.h>
#include <stdio.h>

void	my_true_loop(unsigned int n);

int	my_putchar(char c)
{
write(1, &c, 1);
}

void	my_true_loop(unsigned int n)
{
  unsigned int count;

    count = 0;
    while (count < n)
      {
	my_putchar(43);
	count++;
      }
}
