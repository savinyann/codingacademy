#include <unistd.h>
#include <stdio.h>
#ifndef TEST_H_
#define TEST_H_

struct s_person
{
  char	*first_name;
  char	*last_name;
  int	age;
};

typedef struct s_person t_person;
t_person *new_person(char *first_name, char *last_name, int age);

#endif
